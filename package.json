{
  "name": "cypress-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "merge:reports": "mochawesome-merge cypress/reports/mochawesome-report/*.json > cypress/reports/mochawesome-report/mochawesome.json",
    "generate:report": "marge cypress/reports/mochawesome-report/mochawesome.json -f report -o cypress/reports/mochawesome-report",
    "report": "npm run merge:reports && npm run generate:report"
    
    
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
  "cypress": "^13.17.0"
  },
  "devDependencies": {
    
    "cypress-xpath": "^2.0.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
